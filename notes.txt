ROADMAP
- implement 'all' functions from python math module
- multiple cells (first time kind of a notebook feel)
- now we need to finally make a 'complete' text editor (highlighting, copy, paste, ...)

TODO
- test record feature
- for which values functions like sqrt, log are defined (needs to be checked)

BUGS
- double !
- right shift enter not working

PYTHON MATH MODULE IMPLEMENTATION
skipped:
- math.comb() - we have this with ncr()
- math.copysign
- math.frexp()
- math.fsum()
- math.isclose()
- math.isfinite()
- math.isinf()
- math.isnan()
- math.isqrt()
- math.ldexp()
- math.modf()
- math.nextafter()
progress:
- math.prod() - we need lists before and then we can do sum() next
